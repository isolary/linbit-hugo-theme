---
title: "Design System"
weight: 2
---

This theme consumes the **LINBIT design system** monorepo: `@linbit/tokens`
(the values) and `@linbit/core-css` (framework-agnostic component classes). The
pages below render directly from those vendored assets — change a token upstream,
re-vendor, and everything here moves with it.
